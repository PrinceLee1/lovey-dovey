VITE_PUSHER_APP_KEY=""
VITE_PUSHER_APP_CLUSTER=mt1
VITE_PUSHER_SCHEME=https

# VITE_API_URL must NOT include the /api suffix — it's used to build the
# /broadcasting/auth URL. VITE_API_BASE_URL is the axios base URL and DOES
# include /api. See README.md for details.
VITE_API_URL=https://loveydovey.test
VITE_API_BASE_URL=https://loveydovey.test/api
